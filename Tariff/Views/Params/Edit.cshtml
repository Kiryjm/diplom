@model Tariff.Models.Param

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        @{
            Html.RenderPartial("ParamDataSet", Model);
        }
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
